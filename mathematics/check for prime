 static boolean isprime(int n) {
        if (n == 1) //("one is Neither prime not composite!");
        return false;
        for (int i=2; i<n; i++){
           if (n % i==0)
            return false;
        }
        return true;
    }
